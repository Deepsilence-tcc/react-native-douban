var React = require('react-native');

var {
  StyleSheet,
  Text,
  View,
  ListView,
  Image,
  ScrollView,
  ActivityIndicatorIOS,
  TouchableOpacity
  } = React;

  module.exports = React.createClass({
      render: function(){
        return(
          <View></View>
        );
      }
  });
